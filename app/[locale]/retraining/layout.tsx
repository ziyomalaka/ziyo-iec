import RetrainingShell from "@/components/retraining/RetrainingShell";

export default function RetrainingLayout({ children }: { children: React.ReactNode }) {
  return <RetrainingShell>{children}</RetrainingShell>;
}
